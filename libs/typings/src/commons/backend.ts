export enum BackendFieldTypes {
  BANK = 'bank',
  BOOLEAN = 'boolean',
  COUNTRY = 'country',
  CURRENCY = 'currency',
  DATE = 'date',
  DIALING_CODE = 'dialingCode',
  ENUM = 'enum',
  FLOAT = 'float',
  INSURANCE = 'insurance',
  INTEGER = 'integer',
  SMALLINT = 'smallint',
  STRING = 'string',
  TEXT = 'text',
  TIME = 'time',
}

export enum BackendResource {
  GP_PEOPLE = 'GP_PEOPLE',
  GP_FINTECH = 'GP_FINTECH',
}
